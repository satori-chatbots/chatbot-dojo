info:
  name: List API Keys
  type: http
  seq: 3

http:
  method: GET
  url: "{{host}}/api/api-keys/"
  headers:
    - name: Authorization
      value: Token {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
